import 'package:app_961/router/router.dart';
import 'package:flutter/material.dart';

class App961 extends StatelessWidget {
  const App961({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: '961 Education',
      routes: routes,
    );
  }
}
